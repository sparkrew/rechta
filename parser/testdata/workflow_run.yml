name: Post-CI
on:
  workflow_run:
    workflows: [CI Pipeline, Nightly]
    branches: [main]
    types: [completed]

jobs:
  notify:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - run: echo "Workflow completed successfully"
